{
  "version": "2.0.0",

  "tasks": [
    {
      "label": "Build Testbed (Debug)",
      "type": "shell",

      "command": "cmake",
      "args": ["--build", "${workspaceFolder}/build-debug"]
    },

    {
      "label": "Run Testbed (Debug)",
      "type": "shell",

      "command": "${workspaceFolder}/build-debug/testbed/testbed"
    }
  ]
}