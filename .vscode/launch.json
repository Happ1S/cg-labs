{
  "version": "0.2.0",

  "configurations": [
    {
      "name": "Debug Testbed",
      "type": "cppdbg",
      "request": "launch",

      "program": "${workspaceFolder}/build-debug/testbed/testbed",
      "cwd": "${workspaceFolder}",
      "MIMode": "lldb",

      "preLaunchTask": "Build Testbed (Debug)"
    }
  ]
}